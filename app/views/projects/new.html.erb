<div class="container bg-light">
  <div class="row pt-4">
    <div class="col text-center justify-content-between">
      <h1 class="display-4 d-inline">Create a new Project</h1>
      <br><br>
      <p>Coordinate, track, and update your work in one place, so projects stay transparent and on schedule.</p>
    </div>
  </div>

  <hr>

</div>

<%= render 'form', project: @project %>

<div class="container">
  <div class="form-group my-4">
    <div class="col-sm-offset-2 col-sm-10 mt-3">
      <div class="actions">
        <%= button_to 'Back', projects_path, method: :get, class: "btn" %>
      </div>
    </div>
  </div>
</div>
